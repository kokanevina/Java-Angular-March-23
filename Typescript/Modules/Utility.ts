// module name is same as file name : Utility

export let localTrainType="Harbour";

export function changeTrainType(type:string){
    localTrainType=type;
}

export class Train{
    trainId:string;
    source:string;
    destination:string;
    // constructor, instance methods
}



class Station{
 // iv, c, im
}

export default Station; // in one file we have have only one property as export default